# Override for test profile — no real external connections needed

# Mail — dummy values; EmailService is mocked in tests
spring.mail.host=localhost
spring.mail.port=3025
spring.mail.username=test
spring.mail.password=test
app.mail.from=test@hafsahsplace.com
app.frontend-url=http://localhost:3000

# Paystack — dummy values; Paystack API is not called in unit-scope integration tests
paystack.secret-key=sk_test_dummy_key_for_testing
paystack.public-key=pk_test_dummy_key_for_testing

# Cloudinary — dummy values
cloudinary.cloud-name=test-cloud
cloudinary.api-key=123456789
cloudinary.api-secret=test-secret

# Disable verbose SQL in tests
spring.jpa.show-sql=false
logging.level.com.biliqis.hafsahs_place=WARN
logging.level.org.springframework.security=WARN
